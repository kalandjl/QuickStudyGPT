import setContent from "./setContent"

export default interface setDoc { 
    title: string
    uid: string
    content: setContent
    notes: string
    initialFolder: string
    fullyLoaded: string
}